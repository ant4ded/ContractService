rate:
  starter:
    mode: current-day
    currencies[0]: EUR
    currencies[1]: USD
    enable-cross-rate: true

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    my.idp.spring.currency.rate: debug